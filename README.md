# Zorvyn Assignment ES6 Express Server

## Setup

1. Install dependencies:
   npm install

2. Start the server:
   npm start

- The server runs on port 5000 by default (configurable via .env).
- Test route: GET /api/health → "Server is running"
- Uses ES6 modules and organized folder structure in src/
